# Slack App credentials (from api.slack.com > Your App > Basic Information)
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_SIGNING_SECRET=
SLACK_STATE_SECRET=

# MCP Orchestrator
MCP_SIGNING_SECRET=
MCP_API_URL=https://slack-mcp-orchestrator.fly.dev/api/mcp

# Optional: override for local dev (points generate-config to local MCP client build)
# MCP_CLIENT_PATH=/absolute/path/to/packages/slack-mcp-client/dist/index.js

# Server
PORT=3000
NODE_ENV=development
